spring:
    application:
        name: spring-ai-adviser
    ai:
        openai:
            api-key: ${OPENAI_API_KEY}

logging:
    level:
        org:
            springframework:
                ai:
                    chat:
                        client:
                            advisor: DEBUG
